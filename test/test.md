flowchart LR
    A[Rectangle — process step]
    B(Rounded — start / end)
    C{Diamond — decision}
    D[(Cylinder — database)]
    E((Circle — connector))
    F>Asymmetric — document]
    G[/Parallelogram — input\/output/]